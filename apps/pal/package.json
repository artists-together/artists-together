{
  "name": "@artists-together/pal",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.ts",
    "sync": "node --env-file-if-exists=.env src/scripts/sync.ts",
    "start": "NODE_ENV=production node --env-file-if-exists=.env src/index.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "cron": "^4.4.0",
    "discord.js": "^14.25.1",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
